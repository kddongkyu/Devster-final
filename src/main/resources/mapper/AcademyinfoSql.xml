<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.AcademyinfoMapper">
    <select id="getRoomName" resultType="String" parameterType="int">
        select ai_name from academyinfo where ai_idx=#{ai_idx}
    </select>
</mapper>